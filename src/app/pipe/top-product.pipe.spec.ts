import { TopProductPipe } from './top-product.pipe';

describe('TopProductPipe', () => {
  it('create an instance', () => {
    const pipe = new TopProductPipe();
    expect(pipe).toBeTruthy();
  });
});
