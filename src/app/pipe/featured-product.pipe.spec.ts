import { FeaturedProductPipe } from './featured-product.pipe';

describe('FeaturedProductPipe', () => {
  it('create an instance', () => {
    const pipe = new FeaturedProductPipe();
    expect(pipe).toBeTruthy();
  });
});
