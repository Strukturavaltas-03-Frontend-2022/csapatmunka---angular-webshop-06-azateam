import { RandomProductsPipe } from './random-products.pipe';

describe('RandomProductsPipe', () => {
  it('create an instance', () => {
    const pipe = new RandomProductsPipe();
    expect(pipe).toBeTruthy();
  });
});
